// Stepwise Builder aka Wizard
// used when you need to create elements of an object in order
//   size of the wheels depends on the CarType, so order matters
enum CarType {Sedan, Crossover};
class Car {
  public CarType Type;
  public int WheelSize;
}
class Program {
  static void Main(string[] args) {
  }
}